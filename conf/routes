# Routes


GET     /                                  controllers.HomeController.index()
GET     /account                           controllers.HomeController.account()
GET     /adminpanel                        controllers.HomeController.adminPanel()
GET     /payment                           controllers.HomeController.payment()
GET     /product                           controllers.HomeController.product()
GET     /search                            controllers.HomeController.search()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
